# Track Review — binary_scanner_unification_20260627 — 2026-06-28

## Summary of changes

- **Commit reviewed:** `f933a5e3` — `chore(measure): add binary_scanner_unification_20260627 track`
- **What changed:** A new Measure track was created. The commit added:
  - `measure/tracks/binary_scanner_unification_20260627/metadata.json`
  - `measure/tracks/binary_scanner_unification_20260627/spec.md`
  - `measure/tracks/binary_scanner_unification_20260627/plan.md`
  - An entry in `measure/tracks.md`
- **No source code was modified.** The track is in `planned` status with `actual_tasks: 0`.

The track bundles two related tech-debt items:
1. Renaming the compiled binary from `build-graph` to `repo-graph` and removing the dual-name install workaround.
2. Rewriting `src/build-graph-db.ts` from JSON-batch-insert to `ts-morph`-driven AST extraction, matching the schema defined in `AGENTS.md`.

Both items are documented as Open in `measure/tech-debt.md` (rows dated 2026-05-24 and 2026-06-25), so the track addresses real, tracked debt.

## Spec alignment

**Status: met.**

- The spec uses the classic functional-requirements format, which is appropriate for a chore/tech-debt track.
- Functional requirements (FR-1 through FR-8) are specific, file-level, and traceable to the acceptance-criteria table.
- Non-functional requirements explicitly call for TDD red proofs (FR-5/FR-6/FR-7), no regression in the `agent_explore_freshness_impact_20260622` suite, and a clean `bun run build`.
- Acceptance criteria are concrete and verifiable (`./bin/repo-graph --help` banner, single install target, node/edge counts, FTS invariant, tech-debt rows marked Resolved with SHA evidence).
- Out-of-scope items are listed, including schema changes, new node/edge types, and user migration cleanup.
- The plan mirrors the spec with 5 phases and explicit status checkboxes (`- [ ]`) on every task.

Minor gap: the plan's acceptance-criteria table repeats the spec criteria but does not map individual tasks to ACs. Adding task-to-AC mapping would make progress easier to audit.

## Code quality observations

- **Track metadata is complete.** `metadata.json` includes `track_id`, `type: chore`, `status: planned`, `priority: medium`, `estimated_tasks: 8`, `actual_tasks: 0`, and a concise description.
- **No sprint metadata is present** — correct, because this is a classic chore track, not a story-shaped feature track.
- **Commit message follows Measure convention:** contains the `track_id`, describes the scope, and references the closed tech-debt items.
- **Registry updated:** `measure/tracks.md` now links to the new track and includes a compact rationale.
- **File formatting:** `spec.md` and `plan.md` are missing trailing newlines; `metadata.json` is also missing a trailing newline. This is cosmetic but should be cleaned up in the next commit.
- **AGENTS.md references:** `AGENTS.md` still instructs agents to run `build-graph` commands. FR-3 covers updating these references, so this is expected at track creation, but it is a reminder that the rename must be thorough.

## Risks or blockers

1. **Large blast-radius rename.** Changing the binary name touches `package.json`, build scripts, `.githooks/install.sh`, `README.md`, `AGENTS.md`, and any test or script that invokes `build-graph`. A single missed reference will break installs or CI.
2. **Scanner rewrite is a big, error-prone change.** Moving from JSON ingestion to `ts-morph` AST extraction must preserve (or enrich) node/edge output without regressing the `agent_explore_freshness_impact_20260622` tests. The existing fixture in `__tests__/fixtures/` must be validated before the integration test counts are finalized.
3. **Bundled scope.** The track combines the binary rename and the scanner rewrite because they share the compile artifact. If either piece slips, the whole track slips. The spec already flags a likely deviation (possible `ingest.ts` refactor), which is good, but the implementer should be ready to split if scope grows.
4. **Dependency on schema helpers.** `syncNodeFts` and `recordFileMetadata` are assumed stable. If the new scan path reveals that those helpers need changes, the deviation log must be used and a follow-up track created.

**No severe blockers at track-creation stage.** The spec and plan are sound enough to begin implementation.

## Recommended next actions

1. **Phase 1 — binary rename:**
   - `git mv graphing-tools/build-graph.ts graphing-tools/repo-graph.ts`.
   - Update `package.json` `bin` and build target.
   - Run `rg -l '\bbuild-graph\b'` across the repo and update every remaining reference, especially `AGENTS.md`.
   - Run `bun run build` and `./bin/repo-graph --help` to verify AC-1 and AC-2.
2. **Validate fixture before Phase 3:**
   - Confirm `__tests__/fixtures/` contains a representative fixture repo.
   - Decide expected node/edge counts and document them in the integration test so the RED → GREEN cycle is meaningful.
3. **Baseline regression check:**
   - Run the `agent_explore_freshness_impact_20260622` test suite now to establish a green baseline before the scanner rewrite.
4. **TDD red proof:**
   - Write `tests/integration/ast-scan.test.ts` first and confirm it fails against the current JSON scanner before implementing the `ts-morph` scanner.
5. **Closeout discipline:**
   - Update `measure/tech-debt.md` rows with the closeout commit SHA.
   - Set `metadata.json` `status` to `done` and `actual_tasks` to the real count.
   - Add any deviations encountered (e.g., `ingest.ts` refactor) to `plan.md`.
   - Archive the track via `measure/tracks.md` and the archive directory.
