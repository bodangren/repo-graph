{
  "name": "repo-graph",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "eslint graphing-tools/",
    "doctor": "./measure/doctor.sh",
    "generate": "./measure/generate.sh",
    "build": "bun build --compile ./graphing-tools/build-graph.ts --outfile ./bin/build-graph"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "eslint": "^9.0.0",
    "eslint-plugin-boundaries": "^5.0.0"
  },
  "dependencies": {
    "ts-morph": "^28.0.0"
  }
}
